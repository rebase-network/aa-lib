# 4337 其他模块

## Bundler 模块

Bundler 作为主要代付手段，兼容最新的 P/B，接收/创建区块分离方式，自定义一个内部 UserOperation 内存池，并通过本地 EOA 地址帮用户代付交易，
Bundler 作为发送服务端提供对外接口：

- 接收 UserOperation 接口
- 查询 EntryPoint 状态
- 调试接口等

## 安全后端

主要负责恢复钱包和记录用户操作
负责调度钱包恢复包括社交恢复，专业恢复，对操作安全性进行审查
